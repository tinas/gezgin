const passengerDatabase = require('./passenger-database')
const stationDatabase = require('./station-database')
const bikeDatabase = require('./bike-database')
const scooterDatabase = require('./scooter-database')

module.exports = {passengerDatabase, stationDatabase, bikeDatabase, scooterDatabase}
